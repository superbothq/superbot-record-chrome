RSpec.describe Superbot::Record::Chrome do
  it "has a version number" do
    expect(Superbot::Record::Chrome::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
